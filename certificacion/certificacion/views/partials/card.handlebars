<div class="card mx-auto" style="width: 80%;">
  <img src="/public/img/{{imagen}}" class="card-img-top" alt="{{imagen}}" height="250px">
  <div class="card-body">
    <a  href="/publicacion/{{idPublicacion}}">{{titulo}}</a>.
    <p class="card-text">{{contenido}}</p>
    <p class="card-text">Fecha publicación: {{fecha}}</p>
    <p class="card-text">Autor: {{autor}}</p>
     <p align="center">
    <a  href="/publicacion/{{idPublicacion}}" class="btn btn-primary">Ver detalles</a>
   
   
    </p>
  
  </div>
</div>

